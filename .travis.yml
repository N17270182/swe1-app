language: python

python:
  - 3.8

install:
  - cd mysite
  - pip install -r requirements.txt

script:
  - black --check .
  - flake8 .
  - coverage run --source=mysite_swe1_app,polls manage.py test

after_script:
  - coveralls